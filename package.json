{
  "name": "kagura-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action to trigger Kagura AI test runs",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "package": "npm run build",
    "lint": "node -c src/index.js",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  },
  "engines": {
    "node": ">=20"
  }
}
